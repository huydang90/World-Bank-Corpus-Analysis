---
title: "Thea Cleaned Reps 2012-13"
output:
  html_document:
    df_print: paged
---

### Data loading  
```{r}
library(quanteda)
library(readtext)

require(quanteda)
require(readtext)

path_data <- system.file("/Users/theamadsen/Desktop", package = "readtext")
dat_test_reps_12and13_cleaned <- readtext(paste0(path_data, "Cleaned_reports*"))
                    docvarsfrom = "filenames" 
                    docvarnames = c("unit", "context", "year", "language")
                    dvsep = "_" 
                    encoding = "ISO-8859-1"

str(dat_test_reps_12and13_cleaned)
```


### Making my corpus  
```{r}
doc.corpus <- corpus(dat_test_reps_12and13_cleaned)
summary(doc.corpus)
```

### Cleaning and Creating Tokens  
```{r}
doc.tokens <- tokens(doc.corpus)

doc.tokens <- tokens(doc.tokens, remove_punct = TRUE, 
                     remove_numbers = TRUE)
doc.tokens <- tokens_select(doc.tokens, stopwords('english'),selection='remove')
doc.tokens <- tokens_wordstem(doc.tokens)
```

### Converting to DFM  
```{r}
doc.dfm <- dfm(doc.corpus, remove_numbers = TRUE, 
               stem = TRUE, 
               remove = stopwords("english"))

doc.dfm.final <- dfm(doc.tokens)
```

### Prelim Analysis  
```{r}
head(kwic(doc.tokens, "environment", window = 3))

topfeatures(doc.dfm.final, 5)

topfeatures(doc.dfm.final, 20)

textplot_wordcloud(doc.dfm.final)
```
























